%h1 トップページ
- if user_signed_in?
  %h2 ログインしています
  = link_to "ログアウト", destroy_user_session_path, method: :delete
- else
  %h2 ログインしていません
  = link_to "新規登録", new_user_registration_path
  = link_to "ログイン", new_user_session_path

.wrapper
  = render "number1"
  = render "number2"
  = render "number3"

.new 
  = link_to  "投稿する", new_product_path,  class: "post" 
